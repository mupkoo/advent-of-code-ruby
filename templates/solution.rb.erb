module Aoc<%= @year %>
  class Day<%= @day.to_s.rjust(2, '0') %>
    def solve(part)
      file = File.join(__dir__, 'input.txt')
      input = File.read(file)
      raise 'Empty input.txt file' if input.empty?

      part == 1 ? part1(input) : part2(input)
    end

    def part1(input)
      'not implemented'

    end

    def part2(input)
      'not implemented'
    end

    private

    def parse_input(input)
      input.lines(chomp: true)
    end
  end
end